#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    long long t,num;
    cin >> t;
    while(t--)
    {
        cin >> num;
        int sq=sqrt(num);
        cout << sq <<"\n";
    }
	// your code goes here
	return 0;
}
