#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    long long t;
    while(1)
    {
        cin >> t;
        if(t==42)
        {
            break;
        }
        cout << t << "\n";
    }
	return 0;
}
